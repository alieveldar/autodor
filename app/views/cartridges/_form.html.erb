<%= form_for(@cartridge) do |f| %>
  <% if @cartridge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cartridge.errors.count, "error") %> prohibited this cartridge from being saved:</h2>

      <ul>
      <% @cartridge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :model %><br>
    <%= f.text_field :model %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
