<%= form_for(@day_road_list) do |f| %>
  <% if @day_road_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@day_road_list.errors.count, "error") %> prohibited this day_road_list from being saved:</h2>

      <ul>
      <% @day_road_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'ДЕНЬ' %><br>
    <%= f.date_select :day %> 
    </div>

  <div class="field">
    <%= f.label 'ОБЬЕМ' %><br>
    <%= f.number_field :fuel %>
  </div>
  <div class="field">
    <%= f.label 'АЗС' %><br>
    <%= f.select :station, options_from_collection_for_select(Station.all, :id, :name), prompt: "Выберите АЗС" %>

  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
